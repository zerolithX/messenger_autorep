name: Build Messenger Reply APK

on:
  workflow_dispatch:
  push:
    paths:
      - ".github/workflows/messenger.yml"

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.9"

      - name: Create project files
        run: |
          mkdir -p app/src/main/java/com/example/sleepreply
          mkdir -p app/src/main/res/mipmap-anydpi-v26
          mkdir -p app/src/main/res/drawable-nodpi
          mkdir -p app/src/main/res/values

          base64 -d > app/src/main/res/drawable-nodpi/ic_launcher_foreground.png <<'EOF'
          
          EOF

          cat > app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml <<'EOF'
          <?xml version="1.0" encoding="utf-8"?>
          <adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
              <background android:drawable="@color/ic_launcher_background" />
              <foreground android:drawable="@drawable/ic_launcher_foreground" />
          </adaptive-icon>
          EOF

          cat > app/src/main/res/values/colors.xml <<'EOF'
          <?xml version="1.0" encoding="utf-8"?>
          <resources>
              <color name="ic_launcher_background">#0E0F12</color>
          </resources>
          EOF

          cat > settings.gradle.kts <<'EOF'
          pluginManagement {
              repositories {
                  google()
                  mavenCentral()
                  gradlePluginPortal()
              }
          }
          dependencyResolutionManagement {
              repositories {
                  google()
                  mavenCentral()
              }
          }
          rootProject.name = "MessengerReply"
          include(":app")
          EOF

          cat > build.gradle.kts <<'EOF'
          plugins {
              id("com.android.application") version "8.5.2" apply false
              id("org.jetbrains.kotlin.android") version "1.9.24" apply false
          }
          EOF

          cat > app/build.gradle.kts <<'EOF'
          plugins {
              id("com.android.application")
              id("org.jetbrains.kotlin.android")
          }

          android {
              namespace = "com.example.sleepreply"
              compileSdk = 34

              defaultConfig {
                  applicationId = "com.example.messengerreply"
                  minSdk = 28
                  targetSdk = 34
                  versionCode = 2
                  versionName = "2.0"
              }

              compileOptions {
                  sourceCompatibility = JavaVersion.VERSION_17
                  targetCompatibility = JavaVersion.VERSION_17
              }
              kotlinOptions {
                  jvmTarget = "17"
              }
          }
          EOF

          cat > app/src/main/AndroidManifest.xml <<'EOF'
          <?xml version="1.0" encoding="utf-8"?>
          <manifest xmlns:android="http://schemas.android.com/apk/res/android">

              <uses-permission android:name="android.permission.INTERNET" />

              <application
                  android:icon="@mipmap/ic_launcher"
                  android:label="Messenger Reply"
                  android:theme="@android:style/Theme.Material.Light.DarkActionBar">

                  <activity
                      android:name=".MainActivity"
                      android:exported="true">
                      <intent-filter>
                          <action android:name="android.intent.action.MAIN" />
                          <category android:name="android.intent.category.LAUNCHER" />
                      </intent-filter>
                  </activity>

                  <service
                      android:name=".ReplyService"
                      android:exported="true"
                      android:label="Messenger Reply"
                      android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
                      <intent-filter>
                          <action android:name="android.service.notification.NotificationListenerService" />
                      </intent-filter>
                  </service>
              </application>
          </manifest>
          EOF

          cat > app/src/main/java/com/example/sleepreply/Prefs.kt <<'EOF'
          package com.example.sleepreply

          object Prefs {
              const val NAME = "sleep_reply"
              const val ENABLED = "enabled"
              const val MESSAGE = "message"
              const val START = "start_minutes"
              const val END = "end_minutes"
              const val USE_WINDOW = "use_window"
              const val DEBUG = "debug"

              const val MODE = "mode"
              const val MODE_CUSTOM = "custom"
              const val MODE_AI = "ai"

              const val API_KEY = "gemini_key"
              const val MODEL = "gemini_model"
              const val INSTRUCTIONS = "ai_instructions"

              const val DEFAULT_MESSAGE = "I'm sleeping, catch you later."
              const val DEFAULT_MODEL = "gemini-flash-latest"
              const val DEFAULT_INSTRUCTIONS =
                  "You are replying on behalf of the phone's owner, who is currently unavailable (for example sleeping). " +
                  "Reply in the same language as the incoming message. Keep it short (1-2 sentences), friendly and natural. " +
                  "Do not invent facts or make promises. If the person needs something, say the owner will reply when they are back."
          }
          EOF

          cat > app/src/main/java/com/example/sleepreply/Gemini.kt <<'EOF'
          package com.example.sleepreply

          import org.json.JSONArray
          import org.json.JSONObject
          import java.net.HttpURLConnection
          import java.net.URL

          object Gemini {

              class Turn(val role: String, val text: String)

              class HttpException(val code: Int, message: String) : Exception(message)

              fun reply(apiKey: String, model: String, instructions: String, turns: List<Turn>): String {
                  return try {
                      call(apiKey, model, instructions, turns, true)
                  } catch (e: HttpException) {
                      // some models reject the thinking setting, retry without it
                      if (e.code == 400) call(apiKey, model, instructions, turns, false) else throw e
                  }
              }

              private fun call(
                  apiKey: String,
                  model: String,
                  instructions: String,
                  turns: List<Turn>,
                  thinkingOff: Boolean
              ): String {
                  val merged = ArrayList<Turn>()
                  for (t in turns) {
                      if (merged.isNotEmpty() && merged.last().role == t.role) {
                          val prev = merged.removeAt(merged.size - 1)
                          merged.add(Turn(t.role, prev.text + "\n" + t.text))
                      } else {
                          merged.add(t)
                      }
                  }

                  val contents = JSONArray()
                  for (t in merged) {
                      val part = JSONObject().put("text", t.text)
                      contents.put(
                          JSONObject()
                              .put("role", t.role)
                              .put("parts", JSONArray().put(part))
                      )
                  }

                  val generation = JSONObject()
                      .put("maxOutputTokens", 512)
                      .put("temperature", 0.8)
                  if (thinkingOff) {
                      generation.put("thinkingConfig", JSONObject().put("thinkingBudget", 0))
                  }

                  val system = JSONObject().put(
                      "parts",
                      JSONArray().put(JSONObject().put("text", instructions))
                  )

                  val body = JSONObject()
                      .put("system_instruction", system)
                      .put("contents", contents)
                      .put("generationConfig", generation)

                  val url = URL("https://generativelanguage.googleapis.com/v1beta/models/" + model.trim() + ":generateContent")
                  val conn = url.openConnection() as HttpURLConnection
                  try {
                      conn.requestMethod = "POST"
                      conn.connectTimeout = 15000
                      conn.readTimeout = 30000
                      conn.doOutput = true
                      conn.setRequestProperty("Content-Type", "application/json")
                      conn.setRequestProperty("x-goog-api-key", apiKey.trim())
                      conn.outputStream.use { it.write(body.toString().toByteArray(Charsets.UTF_8)) }

                      val code = conn.responseCode
                      val stream = if (code in 200..299) conn.inputStream else conn.errorStream
                      val response = stream?.bufferedReader()?.use { it.readText() } ?: ""
                      if (code !in 200..299) throw HttpException(code, "HTTP " + code + ": " + response)

                      val candidates = JSONObject(response).optJSONArray("candidates")
                          ?: throw Exception("No reply from Gemini")
                      val parts = candidates.getJSONObject(0)
                          .optJSONObject("content")
                          ?.optJSONArray("parts")
                          ?: throw Exception("Empty reply from Gemini")
                      val sb = StringBuilder()
                      for (i in 0 until parts.length()) {
                          sb.append(parts.getJSONObject(i).optString("text"))
                      }
                      val out = sb.toString().trim()
                      if (out.isEmpty()) throw Exception("Empty reply from Gemini")
                      return out
                  } finally {
                      conn.disconnect()
                  }
              }
          }
          EOF

          cat > app/src/main/java/com/example/sleepreply/ReplyService.kt <<'EOF'
          package com.example.sleepreply

          import android.app.Notification
          import android.app.PendingIntent
          import android.app.RemoteInput
          import android.content.Context
          import android.content.Intent
          import android.os.Bundle
          import android.service.notification.NotificationListenerService
          import android.service.notification.StatusBarNotification
          import java.util.Calendar
          import java.util.concurrent.ConcurrentHashMap
          import java.util.concurrent.Executors

          class ReplyService : NotificationListenerService() {

              private val lastSeen = HashMap<String, String>()
              private val lastReply = ConcurrentHashMap<String, String>()
              private val history = HashMap<String, ArrayList<Gemini.Turn>>()
              private val worker = Executors.newSingleThreadExecutor()

              override fun onDestroy() {
                  worker.shutdown()
                  super.onDestroy()
              }

              override fun onNotificationPosted(sbn: StatusBarNotification) {
                  val prefs = getSharedPreferences(Prefs.NAME, Context.MODE_PRIVATE)
                  if (!prefs.getBoolean(Prefs.ENABLED, true)) return
                  if (sbn.packageName !in listOf("com.facebook.orca", "com.facebook.mlite")) return

                  val n = sbn.notification
                  val extras = n.extras
                  val title = extras.getCharSequence(Notification.EXTRA_TITLE)?.toString()
                      ?: extras.getCharSequence(Notification.EXTRA_CONVERSATION_TITLE)?.toString()
                  val last = lastMessage(extras)
                  val text = extras.getCharSequence(Notification.EXTRA_TEXT)?.toString() ?: last?.first ?: ""
                  val isGroup = extras.getBoolean(Notification.EXTRA_IS_GROUP_CONVERSATION, false)
                  val isSummary = (n.flags and Notification.FLAG_GROUP_SUMMARY) != 0

                  val all = ArrayList<Notification.Action>()
                  n.actions?.let { all.addAll(it) }
                  all.addAll(Notification.WearableExtender(n).actions)

                  val aiMode = prefs.getString(Prefs.MODE, Prefs.MODE_CUSTOM) == Prefs.MODE_AI

                  val info = "app: " + sbn.packageName +
                      "\nmode: " + (if (aiMode) "AI" else "custom") +
                      "\ntitle: " + (if (title == null) "MISSING" else "ok") +
                      "\ntext: " + text.length + " characters" +
                      "\ngroup: " + isGroup + ", summary: " + isSummary +
                      "\nbuttons: " + all.joinToString { describe(it) }

                  fun done(result: String) {
                      prefs.edit().putString(Prefs.DEBUG, info + "\nresult: " + result).apply()
                  }

                  if (prefs.getBoolean(Prefs.USE_WINDOW, true) &&
                      !inSleepWindow(prefs.getInt(Prefs.START, 23 * 60), prefs.getInt(Prefs.END, 7 * 60))
                  ) {
                      return done("outside your sleep hours, so no reply")
                  }
                  if (isSummary) return done("skipped: summary notification")
                  if (isGroup) return done("skipped: group chat")

                  val customMessage = prefs.getString(Prefs.MESSAGE, Prefs.DEFAULT_MESSAGE) ?: Prefs.DEFAULT_MESSAGE
                  if (customMessage.isNotEmpty() && text.contains(customMessage)) return done("skipped: our own reply")

                  val who = title ?: sbn.key
                  val previous = lastReply[who]
                  if (previous != null && (text == previous || text.endsWith(": " + previous))) {
                      return done("skipped: our own reply")
                  }

                  val signature = n.`when`.toString() + "|" + text + "|" + (last?.second ?: 0L)
                  if (lastSeen[who] == signature) return done("skipped: same notification again")

                  val action = all.firstOrNull { a -> a.remoteInputs?.isNotEmpty() == true }
                      ?: return done("no Reply button with a text box in this notification")
                  lastSeen[who] = signature

                  if (!aiMode) {
                      if (customMessage.isBlank()) return done("custom message is empty")
                      if (sendReply(action, customMessage)) {
                          lastReply[who] = customMessage
                          done("replied (custom message)")
                      } else {
                          done("failed: notification expired")
                      }
                      return
                  }

                  if (text.isBlank()) return done("skipped: no message text")
                  val key = prefs.getString(Prefs.API_KEY, "") ?: ""
                  val model = (prefs.getString(Prefs.MODEL, Prefs.DEFAULT_MODEL) ?: "").ifBlank { Prefs.DEFAULT_MODEL }
                  val instructions = (prefs.getString(Prefs.INSTRUCTIONS, Prefs.DEFAULT_INSTRUCTIONS) ?: "")
                      .ifBlank { Prefs.DEFAULT_INSTRUCTIONS }

                  worker.execute {
                      val turns = history.getOrPut(who) { ArrayList() }
                      turns.add(Gemini.Turn("user", text))
                      while (turns.size > 10) turns.removeAt(0)
                      while (turns.isNotEmpty() && turns[0].role != "user") turns.removeAt(0)

                      var error = ""
                      val aiReply: String? = if (key.isBlank()) {
                          error = "no API key saved"
                          null
                      } else {
                          try {
                              Gemini.reply(key, model, instructions, ArrayList(turns))
                          } catch (e: Exception) {
                              error = (e.message ?: "unknown error").take(150)
                              null
                          }
                      }

                      // if AI fails (no internet, bad key, quota), fall back to the custom message
                      val finalReply = aiReply ?: customMessage
                      if (finalReply.isBlank()) return@execute done("AI failed (" + error + ") and custom message is empty")

                      if (sendReply(action, finalReply)) {
                          lastReply[who] = finalReply
                          if (aiReply != null) {
                              turns.add(Gemini.Turn("model", aiReply))
                              done("replied (AI)")
                          } else {
                              done("replied (custom message, AI failed: " + error + ")")
                          }
                      } else {
                          done("failed: notification expired")
                      }
                  }
              }

              private fun sendReply(action: Notification.Action, reply: String): Boolean {
                  val inputs = action.remoteInputs ?: return false
                  val input = inputs.firstOrNull() ?: return false
                  val fill = Intent()
                  val results = Bundle()
                  results.putCharSequence(input.resultKey, reply)
                  RemoteInput.addResultsToIntent(inputs, fill, results)
                  return try {
                      action.actionIntent.send(this, 0, fill)
                      true
                  } catch (e: PendingIntent.CanceledException) {
                      false
                  }
              }

              private fun lastMessage(extras: Bundle): Pair<String, Long>? {
                  val arr = extras.getParcelableArray(Notification.EXTRA_MESSAGES) ?: return null
                  val b = arr.lastOrNull() as? Bundle ?: return null
                  return Pair(b.getCharSequence("text")?.toString() ?: "", b.getLong("time"))
              }

              private fun describe(a: Notification.Action): String {
                  val hasInput = a.remoteInputs?.isNotEmpty() == true
                  return "" + a.title + " (text box: " + hasInput + ")"
              }

              private fun inSleepWindow(start: Int, end: Int): Boolean {
                  val c = Calendar.getInstance()
                  val now = c.get(Calendar.HOUR_OF_DAY) * 60 + c.get(Calendar.MINUTE)
                  return if (start <= end) now in start until end else (now >= start || now < end)
              }
          }
          EOF

          cat > app/src/main/java/com/example/sleepreply/MainActivity.kt <<'EOF'
          package com.example.sleepreply

          import android.app.Activity
          import android.app.TimePickerDialog
          import android.content.Context
          import android.content.Intent
          import android.content.SharedPreferences
          import android.os.Bundle
          import android.provider.Settings
          import android.text.InputType
          import android.view.View
          import android.widget.Button
          import android.widget.EditText
          import android.widget.LinearLayout
          import android.widget.RadioButton
          import android.widget.RadioGroup
          import android.widget.ScrollView
          import android.widget.Switch
          import android.widget.TextView
          import android.widget.Toast

          class MainActivity : Activity() {

              private lateinit var prefs: SharedPreferences
              private lateinit var accessStatus: TextView
              private lateinit var debugView: TextView

              override fun onCreate(savedInstanceState: Bundle?) {
                  super.onCreate(savedInstanceState)
                  prefs = getSharedPreferences(Prefs.NAME, Context.MODE_PRIVATE)

                  val pad = (16 * resources.displayMetrics.density).toInt()
                  val root = LinearLayout(this)
                  root.orientation = LinearLayout.VERTICAL
                  root.setPadding(pad, pad, pad, pad)

                  fun label(t: String): TextView {
                      val v = TextView(this)
                      v.text = t
                      v.setPadding(0, pad / 2, 0, 0)
                      return v
                  }

                  val heading = TextView(this)
                  heading.text = "Messenger Reply"
                  heading.textSize = 24f
                  root.addView(heading)

                  accessStatus = TextView(this)
                  root.addView(accessStatus)

                  val accessButton = Button(this)
                  accessButton.text = "Open notification access settings"
                  accessButton.setOnClickListener {
                      startActivity(Intent(Settings.ACTION_NOTIFICATION_LISTENER_SETTINGS))
                  }
                  root.addView(accessButton)

                  val enabled = Switch(this)
                  enabled.text = "Auto-reply on"
                  enabled.isChecked = prefs.getBoolean(Prefs.ENABLED, true)
                  enabled.setOnCheckedChangeListener { _, on ->
                      prefs.edit().putBoolean(Prefs.ENABLED, on).apply()
                  }
                  root.addView(enabled)

                  val windowSwitch = Switch(this)
                  windowSwitch.text = "Only reply during the time window"
                  windowSwitch.isChecked = prefs.getBoolean(Prefs.USE_WINDOW, true)
                  windowSwitch.setOnCheckedChangeListener { _, on ->
                      prefs.edit().putBoolean(Prefs.USE_WINDOW, on).apply()
                  }
                  root.addView(windowSwitch)

                  val startButton = Button(this)
                  val endButton = Button(this)
                  fun refresh() {
                      startButton.text = "Start: " + format(prefs.getInt(Prefs.START, 23 * 60))
                      endButton.text = "End: " + format(prefs.getInt(Prefs.END, 7 * 60))
                  }
                  startButton.setOnClickListener { pickTime(Prefs.START, 23 * 60) { refresh() } }
                  endButton.setOnClickListener { pickTime(Prefs.END, 7 * 60) { refresh() } }
                  refresh()
                  root.addView(startButton)
                  root.addView(endButton)

                  // ---- reply mode ----
                  root.addView(label("Reply mode"))
                  val group = RadioGroup(this)
                  val rbCustom = RadioButton(this)
                  rbCustom.id = View.generateViewId()
                  rbCustom.text = "Custom message"
                  val rbAi = RadioButton(this)
                  rbAi.id = View.generateViewId()
                  rbAi.text = "AI reply (Gemini)"
                  group.addView(rbCustom)
                  group.addView(rbAi)
                  val aiOn = prefs.getString(Prefs.MODE, Prefs.MODE_CUSTOM) == Prefs.MODE_AI
                  group.check(if (aiOn) rbAi.id else rbCustom.id)
                  root.addView(group)

                  // ---- AI settings ----
                  val aiBox = LinearLayout(this)
                  aiBox.orientation = LinearLayout.VERTICAL
                  aiBox.visibility = if (aiOn) View.VISIBLE else View.GONE

                  aiBox.addView(label("Gemini API key"))
                  val keyInput = EditText(this)
                  keyInput.hint = "Paste your API key"
                  keyInput.inputType = InputType.TYPE_CLASS_TEXT or InputType.TYPE_TEXT_VARIATION_PASSWORD
                  keyInput.setText(prefs.getString(Prefs.API_KEY, "") ?: "")
                  aiBox.addView(keyInput)

                  aiBox.addView(label("Model"))
                  val modelInput = EditText(this)
                  modelInput.inputType = InputType.TYPE_CLASS_TEXT
                  modelInput.setText(prefs.getString(Prefs.MODEL, Prefs.DEFAULT_MODEL) ?: Prefs.DEFAULT_MODEL)
                  aiBox.addView(modelInput)

                  aiBox.addView(label("AI instructions (how it should reply)"))
                  val instrInput = EditText(this)
                  instrInput.inputType = InputType.TYPE_CLASS_TEXT or InputType.TYPE_TEXT_FLAG_MULTI_LINE
                  instrInput.minLines = 4
                  instrInput.setText(prefs.getString(Prefs.INSTRUCTIONS, Prefs.DEFAULT_INSTRUCTIONS) ?: Prefs.DEFAULT_INSTRUCTIONS)
                  aiBox.addView(instrInput)

                  val test = Button(this)
                  test.text = "Test AI reply"
                  test.setOnClickListener {
                      val key = keyInput.text.toString().trim()
                      val model = modelInput.text.toString().trim().ifEmpty { Prefs.DEFAULT_MODEL }
                      if (key.isEmpty()) {
                          Toast.makeText(this, "Enter your API key first", Toast.LENGTH_SHORT).show()
                          return@setOnClickListener
                      }
                      Toast.makeText(this, "Testing...", Toast.LENGTH_SHORT).show()
                      Thread {
                          val result = try {
                              "AI says: " + Gemini.reply(
                                  key,
                                  model,
                                  "Reply with one short friendly sentence.",
                                  listOf(Gemini.Turn("user", "Hi, are you there?"))
                              )
                          } catch (e: Exception) {
                              "Failed: " + (e.message ?: "unknown error").take(200)
                          }
                          runOnUiThread { Toast.makeText(this, result, Toast.LENGTH_LONG).show() }
                      }.start()
                  }
                  aiBox.addView(test)
                  root.addView(aiBox)

                  group.setOnCheckedChangeListener { _, id ->
                      val ai = id == rbAi.id
                      prefs.edit().putString(Prefs.MODE, if (ai) Prefs.MODE_AI else Prefs.MODE_CUSTOM).apply()
                      aiBox.visibility = if (ai) View.VISIBLE else View.GONE
                  }

                  // ---- custom message ----
                  root.addView(label("Custom message (also sent if the AI fails)"))
                  val message = EditText(this)
                  message.setText(prefs.getString(Prefs.MESSAGE, Prefs.DEFAULT_MESSAGE) ?: Prefs.DEFAULT_MESSAGE)
                  root.addView(message)

                  val save = Button(this)
                  save.text = "Save settings"
                  save.setOnClickListener {
                      prefs.edit()
                          .putString(Prefs.MESSAGE, message.text.toString())
                          .putString(Prefs.API_KEY, keyInput.text.toString().trim())
                          .putString(Prefs.MODEL, modelInput.text.toString().trim().ifEmpty { Prefs.DEFAULT_MODEL })
                          .putString(Prefs.INSTRUCTIONS, instrInput.text.toString().ifBlank { Prefs.DEFAULT_INSTRUCTIONS })
                          .apply()
                      Toast.makeText(this, "Saved", Toast.LENGTH_SHORT).show()
                  }
                  root.addView(save)

                  val note = TextView(this)
                  note.text = "Replies to Messenger messages (not group chats). In AI mode the incoming message text is sent to Google Gemini to write the reply."
                  note.setPadding(0, pad / 2, 0, 0)
                  root.addView(note)

                  val debugLabel = TextView(this)
                  debugLabel.text = "Last notification seen (for troubleshooting)"
                  debugLabel.setPadding(0, pad, 0, 0)
                  root.addView(debugLabel)

                  debugView = TextView(this)
                  debugView.setTextIsSelectable(true)
                  root.addView(debugView)

                  val scroll = ScrollView(this)
                  scroll.addView(root)
                  setContentView(scroll)
              }

              override fun onResume() {
                  super.onResume()
                  debugView.text = prefs.getString(Prefs.DEBUG, "Nothing seen yet.")
                  val list = Settings.Secure.getString(contentResolver, "enabled_notification_listeners") ?: ""
                  accessStatus.text = if (list.contains(packageName)) {
                      "Notification access: ON"
                  } else {
                      "Notification access: OFF. Tap the button below and allow Messenger Reply."
                  }
              }

              private fun pickTime(key: String, def: Int, done: () -> Unit) {
                  val cur = prefs.getInt(key, def)
                  TimePickerDialog(this, { _, h, m ->
                      prefs.edit().putInt(key, h * 60 + m).apply()
                      done()
                  }, cur / 60, cur % 60, true).show()
              }

              private fun format(mins: Int): String = String.format("%02d:%02d", mins / 60, mins % 60)
          }
          EOF

      - name: Use fixed signing key
        run: |
          mkdir -p ~/.android
          base64 -d > ~/.android/debug.keystore <<'EOF'
          
          EOF

      - name: Build APK
        run: gradle assembleDebug --no-daemon

      - name: Save APK as artifact
        uses: actions/upload-artifact@v4
        with:
          name: MessengerReply-APK
          path: app/build/outputs/apk/debug/app-debug.apk

      - name: Publish APK to Releases
        env:
          GH_TOKEN: ${{ github.token }}
        run: >
          gh release create "messenger-v${{ github.run_number }}"
          app/build/outputs/apk/debug/app-debug.apk
          --repo "${{ github.repository }}"
          --target "${{ github.sha }}"
          --title "Messenger Reply build ${{ github.run_number }}"
